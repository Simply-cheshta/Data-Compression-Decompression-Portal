<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Compression Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow">
      <div class="card-body">
        <h3 class="card-title text-success">Compression Complete</h3>
        <ul class="list-group list-group-flush my-4">
          <li class="list-group-item"><strong>Original File:</strong> <%= originalFile %></li>
          <li class="list-group-item"><strong>Compressed File:</strong> <%= outputFile %></li>
          <li class="list-group-item"><strong>Original Size:</strong> <%= originalSize %> bytes</li>
          <li class="list-group-item"><strong>Compressed Size:</strong> <%= compressedSize %> bytes</li>
          <li class="list-group-item"><strong>Compression Ratio:</strong> <%= compressionRatio %>%</li>
          <li class="list-group-item"><strong>Processing Time:</strong> <%= processingTime %> ms</li>

        </ul>
        <% if (typeof warning !== 'undefined' && warning) { %>
          <div class="alert alert-warning"><%= warning %></div>
        <% } %>
        <div class="d-flex justify-content-between mt-4">
          <a href="/outputs/<%= outputFile %>" class="btn btn-primary" download>⬇ Download Compressed File</a>
          <a href="/" class="btn btn-secondary">← Back to Upload</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
